npm init -Y  //create package.json
npm i express mongoose nodemon jsonwebtoken bcryptjs cookie-parser cors //install all packages
npm run dev // http://localhost:8000/

Username: pradnyajadhav995
Password: SSfe1AVPorWBHche
-------------------------------------
cd backend
npm i datauri
npm i cloudinary
npm i multer

---------------------------------- Frontend ---------------------------------------
cd frontend
npm create vite@latest
npm add -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
<!-- npx shadcn-ui@latest init
npx shadcn-ui@latest add button -->
vite.config.js, tailwind.config.js and App.css code added

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
    theme: {
    extend: {},
  },
  plugins: [],
}

if App.jsx error occure after installed tailwind then add following code in eslint.config.js file    // 'react/react-in-jsx-scope': 'off',
  'no-unused-vars': [
    'warn',
    { varsIgnorePattern: '^React$' } // Ignore unused React variable //$
  ],

npm install shadcn or npm install -g shadcn
npx shadcn@latest init
npx shadcn@latest add button
npx shadcn@latest add label
npm i react-router-dom
npx shadcn@latest add sonner
npm i axios
npx shadcn@latest add avatar
npx shadcn@latest add dialog
npx shadcn@latest add textarea
npx shadcn@latest add badge
npx shadcn@latest add select
npx shadcn@latest add popover
npm i socket.io
npm i socket.io-client

% redux toolkit installation
npm install @reduxjs/toolkit react-redux
% In src/redux/store.js
const store = configureStore({
    reducer:{
        auth:authSlice
    },
    devTools: window.__REDUX_DEVTOOLS_EXTENSION__ && window.__REDUX_DEVTOOLS_EXTENSION__() % add this code
});
% install redux persist
npm i redux-persist
--------------------------------------------------------------------------------------
git remote add origin https://github.com/pradnyashilaNavn/Instagram.git
git remote set-url origin https://github.com/pradnyashilaNavn/Instaclone.git
% Tsting Login Credentials
"email":"pradnya@gmail.com",
"password":"pradnya"

% Pending Task
- message Notification
- Unfollow/follow user function call at home page 
- after watched Notification it should be removed from notification list.
- fix bug in realtime chat 
- Password show hide
- make login page responsive

% Advance Task

- bio function while adding post
- search function
- suggested for you relative and not folllow show all 
